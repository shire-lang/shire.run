---
interface Props {
    placeholder: string;
    className: string;
}

const { className, placeholder } = Astro.props;
---

<input class:list={["border border-gray-300 rounded-md p-2", className]} type="text" placeholder={placeholder} />
